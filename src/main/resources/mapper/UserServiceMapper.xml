<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssm.dao.UserService">

    <select id="selectUserByEmail" resultMap="UserResultMap" parameterType="java.lang.String">
        SELECT user.uid,user.name,user.password,user.salt,user.email,user.phone,
        user.gender,user.avatar,user.signup_at,user.address

        FROM user
        WHERE user.email = #{email}
    </select>

    <select id="selectUserByIdNotAllData" resultMap="UserResultMap" parameterType="long">
        SELECT user.uid,user.name,user.email,user.gender,user.avatar
        FROM user
        WHERE user.email = #{uid}
    </select>

    <resultMap id="UserResultMap" type="com.ssm.model.User">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="uid" jdbcType="BIGINT" property="uid" />
        <result column="name" jdbcType="VARCHAR" property="name" />
        <result column="salt" jdbcType="VARCHAR" property="salt" />
        <result column="password" jdbcType="VARCHAR" property="password" />
        <result column="email" jdbcType="VARCHAR" property="email" />
        <result column="phone" jdbcType="VARCHAR" property="phone" />
        <result column="gender" jdbcType="VARCHAR" property="gender" />
        <result column="avatar" jdbcType="VARCHAR" property="avatar" />
        <result column="signup_at" jdbcType="BIGINT" property="signupAt" />
        <result column="address" jdbcType="VARCHAR" property="address" />
    </resultMap>


</mapper>